# Backend API URL — replace with your actual deployed backend URL
# Get this from: Azure Portal → App Service → Overview → Default domain
# This file is for local reference only. In CI/CD, VITE_API_URL is
# passed via the GitHub variable of the same name.
VITE_API_URL=https://your-app-name.azurewebsites.net
